<?php

/**
 * @file
 * Install, requirements functions for the sprowt_ckeditor_columns module.
 */

/**
 * Implements hook_requirements().
 */
function sprowt_ckeditor_columns_requirements($phase) {
  $requirements = [];

  if ($phase == 'install' || $phase == 'runtime') {

    // Is the library found in the root libraries path.
    $library_found = file_exists(DRUPAL_ROOT . '/libraries/ckeditor/plugins/sprowtcolumns');

    if ($library_found) {
      $requirements['sprowtcolumns'] = [
        'title' => t('sprowtcolumns'),
        'value' => t('Plugin detected'),
        'severity' => REQUIREMENT_OK,
      ];
    }
    else {
      $requirements['sprowtcolumns'] = [
        'title' => t('Sprowt CKEditor Columns'),
        'value' => t('Sprowt CKEditor Columns library missing'),
        'severity' => REQUIREMENT_ERROR,
        'description' => t(''),
      ];
    }
  }

  return $requirements;

}